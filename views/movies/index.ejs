<!DOCTYPE html>
<%- include('../partials/navbar.ejs', {
    user: user
    }) %>


    <h1 class="recentUpdatesDisplay">Recently Updated Films</h1>
    <div class="goToNewMoviePage"> 
        <a href="/movies/new">Add New Movie</a>
    </div>
            <% for(let i = 0; i < movies.length; i++){ %>
                <a href="/movies/<%=movies[i]._id%>">
                    <img src="<%= movies[i].posterUrl %>" />
                </a>
            <%}%>
    </div>
</body>
</html>