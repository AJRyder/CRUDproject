<!DOCTYPE html>
<%- include('../partials/navbar.ejs', {
    user: currentUser
    }) %>
  <body>
    <div class="userContainer">
        <div class="usersOverview">
            </br> <% for(let i =0; i < users.length; i++){%>
                <a class = "userNameList" href="/users/<%=users[i]._id%>">
            </br><%=users[i].username%></br><img class="profilePic" src=<%=users[i].profilePic%>></br>
                </a>
            <%}%>
        </div>
    </div>
   
</body>
</html>